export default function NotFound({ ref }) {
    
    return (
        <section ref={ref}>
            <h1>Not found</h1>
            <p>The provided URL does not exists.</p>
        </section>
    )
}